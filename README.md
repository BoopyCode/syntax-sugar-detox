# Syntax Sugar Detoxifier

Removes modern JavaScript syntactic sugar to reveal the underlying code. Because sometimes you need to see what's actually happening. Run: `node syntax-sugar-detox.js your-file.js`